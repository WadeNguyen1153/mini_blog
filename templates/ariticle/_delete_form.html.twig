<form method="post" action="{{ path('app_ariticle_delete', {'id': ariticle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ariticle.id) }}">
    <button class="btn">Delete</button>
</form>
